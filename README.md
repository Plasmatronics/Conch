# FamilyTreeApp

This will be the README for the Family Tree App

conventions:
prefer render logic to be inline inside react functional component returns
private interfaces start with "I"
prefer object imports over named imports from packages ie import react from "react" instead of import react, {useState, useRef, useEffect} from "react"
